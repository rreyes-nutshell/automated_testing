# Example environment configuration
# Copy this file to 'instance/.env' and adjust values as needed

# Secret key used by Flask sessions
FLASK_SECRET_KEY=change_me

# Database connection URL
DATABASE_URL=postgresql://user:password@localhost/dbname

# Enable verbose debug logging when set to true
DEBUG_MODE=false
# Launch Playwright in headless mode when true
HEADLESS=true
